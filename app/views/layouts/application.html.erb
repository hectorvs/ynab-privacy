<!DOCTYPE html>
<html>
<head>
  <title>YnabPrivacy</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%=  yield :head %>
</head>

<body>
<nav class="navbar navbar-inverse bg-primary-indigo">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'YNAB - Privacy Linker', root_path, class: 'navbar-brand' %>
    </div>
  </div>
</nav>

<div class="container">
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
      <%= value %>
    </div>
  <% end %>

  <%= yield %>
</div>

</body>
</html>
